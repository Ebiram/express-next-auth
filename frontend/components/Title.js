import React from 'react'

const Title = ({ title }) => {
    return (
        <h1 className="font-bold text-center text-5xl text-black mb-10">
            {title}
        </h1>
    )
}

export default Title